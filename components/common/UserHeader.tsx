

import AppText from "./AppText";
import { View } from "react-native";

interface UserHeaderProps {
    
}

const UserHeader = ({
    
}: UserHeaderProps) => {
    return (
        <View>
            <AppText>UserHeader</AppText>
        </View>
    )
}